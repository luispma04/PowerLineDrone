<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".ConnectionActivity">

    <TextView
        android:id="@+id/tv_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="100dp"
        android:gravity="center"
        android:text="Drone Inspection Application"
        android:textSize="24sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/tv_connection_status"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/tv_title"
        android:layout_marginTop="50dp"
        android:gravity="center"
        android:text="Status: Disconnected"
        android:textSize="18sp" />

    <TextView
        android:id="@+id/tv_connection_hint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/tv_connection_status"
        android:layout_marginTop="20dp"
        android:gravity="center"
        android:text="Please connect your drone to continue"
        android:textSize="16sp" />

    <Button
        android:id="@+id/btn_open"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/tv_connection_hint"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"
        android:text="Open"
        android:enabled="false" />

</RelativeLayout>